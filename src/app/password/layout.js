export const metadata = {
  title: "Generatore di Password Sicure Offline — NoMarkPass",
  description:
    "Genera password sicure con entropia hardware locale, direttamente nel browser: zero server, zero tracciamento, zero rischi.",
}

export default function PasswordLayout({ children }) {
  return children
}